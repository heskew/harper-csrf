# Harper CSRF Plugin - Environment Variables
# Copy this file to .env and fill in your values

# ===========================================
# CSRF Configuration (optional)
# ===========================================
# The CSRF plugin works with defaults and doesn't require secrets.
# These environment variables are optional if you want to customize
# the configuration without modifying config.yaml directly.

# Token length in bytes (default: 32)
# CSRF_TOKEN_LENGTH=32

# HTTP header name for CSRF token (default: x-csrf-token)
# CSRF_HEADER_NAME=x-csrf-token

# Form body field name for CSRF token (default: _csrf)
# CSRF_BODY_FIELD=_csrf

# Session key for storing the token (default: csrfToken)
# CSRF_SESSION_KEY=csrfToken
